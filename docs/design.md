# 設計

> ✅ **2026-07-03 ステータス: C 戦略（MA クロス）採用、Phase 1 小額実運用中（2026-06-04〜）**
> A1 単一銘柄押し目 (PF 0.43)、B 2銘柄分散押し目 (PF 0.61) は撤退。
> **C 戦略 = 5×25日 MA クロス + 25日MA 上向きフィルタ + 出来高サージ + 2銘柄分散 + ¥600,000**（2026-07-05 最終増額）。
> 出口は **noTP/ATR3.0/TS5**（利確なし）— 拡張グリッド WF 3/3 Robust、固定4年適用 平均+1.40%/月（backtest_report §8.14）。
> application 層（daily_runner / pipelines / sheets / slack）の複数 Position 対応は実装済み（`eb56853`→`1e91389`）。
> 詳細: [docs/backtest_report.md §8.7〜8.11](backtest_report.md)

> **関連ドキュメント**
> - [docs/requirements.md](requirements.md) — 要件（投資方針・リスク制約・段階拡大プラン）
> - [docs/backtest_report.md](backtest_report.md) — バックテスト結果報告書（A1/B 撤退、C 採用）
> - [docs/operator_guide.md](operator_guide.md) — 日次運用手順（C 戦略・複数銘柄対応版）

## 戦略コンセプト

本システムは「**5×25日 MA クロスで上昇トレンド開始を検知し、上向き中期 MA と出来高サージで実需を確認したうえで 2銘柄分散保有する**」設計です（トレンドフォロー型）。

「下がっている中で買う」（押し目モメンタム）ではなく「**上がり始めた確認後に買う**」アプローチ。短期 MA が中期 MA を上抜けて、かつ中期 MA 自身が上向きの局面に絞ることで、ダマシブレイクと「落ちてくるナイフ」掴みの両方を回避する。

各銘柄独立で最大5営業日のタイムストップ・損切り逆指値（-2.5%）・3.0×ATR のトレーリングで機械的に手仕舞う。**固定利確（TP）は置かない** — トレンドフォローでは固定利確が勝ちトレードを切ることが実測で確認されたため（2026-07-05 改定）。

**分散方針（段階拡大）**:
- **現行（¥600,000 固定）**: 最大 **2 銘柄**同時保有（1銘柄あたり予算 ≒ ¥300,000、対象株価 ¥100〜¥3,000）
- **Phase 2（¥300,000）**: 最大 **3 銘柄**同時保有
- **Phase 3（¥1,000,000）**: 最大 **3 銘柄**同時保有

単元株（100株単位）取引を使用し、SBI証券の逆指値注文でザラ場の損切り（ハード/トレーリング）を**各銘柄独立で**機械化する。

> **押し目モメンタム（A1/B 戦略）からの転換根拠**
> - A1 単一銘柄: OOS 2023 PF 0.43、OOS 2025 PF 0.77 → 2/2 窓 FAIL
> - B 2銘柄分散押し目: OOS 2023 PF 0.61 → ❌ FAIL（分散効果はあったが基準未達）
> - C 2銘柄分散 MA クロス: OOS 2023 PF 0.85、OOS 2024 PF 1.21 ✅、OOS 2025 PF 1.36 ✅ → 2/3 窓 PASS
> - 「下がってる中で買う」自体が日本小型株では構造的に不利、「上がり始め確認」への転換で改善

---

## スクリーニング条件

流動性の低い銘柄・財務的に脆弱な銘柄・割高銘柄を事前に除外し、シグナルの誤検知を減らす。

| 項目 | 条件（Phase 1） | 根拠 |
|---|---|---|
| 株価 | **¥100 〜 ¥3,000** | 1銘柄予算 ¥600k/2 = ¥300k → 100株上限 ¥3,000。下限¥100はスプレッド過大回避。旧上限 ¥1,000 によるユニバース痩せ細りは 2026-07-05 増額で解消 |
| 時価総額 | ¥30億以上 | 極小型株はニュース1本で急騰急落しやすく、テクニカル分析が機能しにくい |
| 流動性 | 5日平均売買代金 ¥1億以上 | 売買代金が薄い銘柄は逆指値発動時のスリッページが大きくなる |
| PBR | 0.5 〜 2.0 倍 | 0.5倍未満は解散価値割れ（業績悪化懸念）、2.0倍超は割高 |
| 自己資本比率 | 30%以上 | 30%未満は財務レバレッジが高く、外部環境の変化に脆弱 |
| 決算ブラックアウト | ±3営業日は除外 | 決算前後のギャップリスクを回避 |

> **用語**
> - *PBR（株価純資産倍率）*: 株価 ÷ 1株当たり純資産。企業の解散価値に対する割安・割高の目安。
> - *自己資本比率*: 純資産 ÷ 総資産。高いほど借金が少なく財務的に安定。

---

## シグナル条件（C 戦略 = MA クロス + トレンドフィルタ）

3条件すべてを満たした銘柄のみをエントリー候補とする。

| 条件 | 内容 | 根拠 |
|---|---|---|
| ① クロス | **5日MA > 25日MA** | 短期が中期を上抜け = 上昇基調確認（疑似ゴールデンクロス継続条件） |
| ② 中期トレンド | **25日MA(today) > 25日MA(today - 20)** | 中期トレンド自身が上向きであることをフィルタ、ダマシ排除 |
| ③ 出来高サージ | 当日出来高 > 20日平均出来高 × 1.2 | 実需を伴う動きを確認、ダマシブレイク抑制 |

> **押し目モメンタム（A1/B）からの撤廃条件**
> - RSI 押し目ゾーン (35-60): 撤廃 — 「下がっている中で買う」概念を捨てる
> - 75日MA 上のトレンド条件: 撤廃 — 25日MA 上向きで代替
> - ボラ収縮（20日 std < 過去平均）: 撤廃 — 上昇トレンド中はボラが拡大することがあるため

> **C 採用までの履歴**
> 1. **旧S株版「RSI(5)<45 + 終値>5日MA」**: RSI(5)が短期反転で急上昇するため数学的に成立せず。実ライブ（2026-05-22）スクリーナー通過29件中シグナル0件。
> 2. **A1: 押し目モメンタム RSI(14) 35-60 + 75日MA上 + ボラ収縮 + 出来高サージ**: Grid Search 月+0.40%/PF 1.20 だが Walk-Forward で過剰最適化検出、撤退。
> 3. **B: 同じシグナル + 2銘柄分散 ¥200k**: PF 0.43→0.61 と改善も robust 未達、撤退。
> 4. **C: 5×25日 MA クロス + 25日MA 上向き + 出来高サージ + 2銘柄分散 ¥200k**: 三窓 2/3 ✅ Robust 確定、採用。

> **用語**
> - *MA（移動平均）*: 過去N日間の終値の平均。トレンド方向の平滑化。5日MA と 25日MA の差分が短期勢いを示す。
> - *MA 上向き*: 当日 MA 値が N 日前 MA 値より大きい状態。トレンドの傾きが上方であることを意味する。
> - *出来高サージ*: 直近出来高が平均より高い状態。需給逼迫・実需介入の指標。

候補が複数の場合は **RSI昇順 → 出来高サージ率降順 → PBR昇順** でランキングし、**空きスロット数まで（Phase 1: 最大2件、Phase 2/3: 最大3件）を翌営業日のエントリー候補としてキューに入れる**。
※ RSI はランキング順位付けには使うが、C 戦略のフィルタには使わない（押し目モメンタムからの差分）。

**保有銘柄の重複排除**: スクリーナー段階で既保有 ticker（`held_tickers` セット）はフィルタアウトする。同一銘柄を2スロットに入れることはしない（損益管理と分散効果の崩壊を防ぐ）。

---

## 発注数量（動的資金分配）

```
1銘柄予算 = floor(残キャッシュ / 残空きスロット数)
最悪ケース執行価格 = 前日終値 × (1 + 2%)  ← ギャップアップ上限
最大株数 = floor(1銘柄予算 / 最悪ケース執行価格)
発注株数 = floor(最大株数 / 100) × 100      ← 単元株なので100株単位
```

- **残キャッシュ** = 投資資本 - 既保有 Position の合計 cost_basis
- **残空きスロット数** = MAX_POSITIONS - 既保有 Position 数（Phase 1: 2 - n）

¥600,000・同時保有 2銘柄でスロット 0 件埋まっている状態で株価¥1,400の銘柄をエントリーする例:
- 1銘柄予算 = floor(600,000 / 2) = ¥300,000
- 最悪ケース執行価格 = 1,400 × 1.02 = 1,428
- 最大株数 = floor(300,000 / 1,428) = 210
- 発注株数 = floor(210 / 100) × 100 = **200株**
- 実投下額 ≈ ¥280,000、残キャッシュ ≈ ¥320,000（次の1銘柄予算は ¥320,000 / 1 = ¥320,000）

---

## 発注フロー

### エントリー（1〜N銘柄、N = 空きスロット数）

| ステップ | 時刻 | 担当 | 内容 |
|---|---|---|---|
| 1 | 当日 20:30 | システム | 空きスロット数までシグナルをランキング上位順に検出 → Slack通知（銘柄ごとに 推奨株数・想定エントリー価格・**推奨ストップ価格** を一括表示。利確価格は廃止） |
| 2 | 翌営業日 9:00 | オペレーター | **各銘柄独立に**始値確認。下記いずれかでその銘柄のエントリー見送り:<br>・始値 > 前日終値 × 1.02（ギャップアップ）<br>・始値 ≤ 推奨ストップ価格（ギャップダウンで即ストップ圏内）<br>他の銘柄は通常通り進行 |
| 3 | 9:00以降の任意時刻 | オペレーター | SBI証券で**銘柄ごとに成行買い**発注 → 即時約定 |
| 4 | 約定直後 | オペレーター | **銘柄ごとに**SBI証券で損切り逆指値をセット:<br>・逆指値: エントリー価格 × 0.975（-2.5%）、トリガー後成行<br>（利確指値は置かない = TP 廃止、OCO 不要） |
| 5 | 約定情報 | オペレーター | Slack の専用スレッドに**全銘柄の**約定価格・株数を返信 → システムが Sheets に複数行で保存 |

### 保有中の出口管理（各銘柄独立）

| 出口 | 発動 | 実装 |
|---|---|---|
| ハードストップ -2.5% | 各銘柄でザラ場価格到達 | 逆指値で**自動執行** |
| トレーリングストップ（3.0×ATR） | 毎営業日 20:30 に**各銘柄独立に**システムが計算 | Slack通知 → オペレーターが該当銘柄の SBI 逆指値価格を切り上げ（再セット） |
| タイムストップ（5営業日） | 各銘柄ごとに 5営業日経過 | 5日目の引け後にシステムが通知 → オペレーターが該当銘柄の逆指値キャンセル＋翌朝成行売却 |

**出口で空いたスロットの再充填**: 出口当日の引け後シグナル検出フェーズで、その銘柄を含めた残スロット分のエントリー候補をキューに入れる。翌営業日 9:00 始値で新規エントリーが行われる（SBI現物取引のため、売却資金は翌朝の買付余力に反映される）。

---

## 出口ルール詳細

### 優先順位

| 優先度 | ルール | 発動条件 | 実装 |
|---|---|---|---|
| 1 | ハードストップロス | 株価 ≤ エントリー価格 × (1 - 2.5%) | SBI逆指値 |
| 2 | トレーリングストップ | 株価 ≤ max(ハードストップ, 直近高値 - 3.0×ATR(14)) | システム計算 → 手動でSBI逆指値更新 |
| 3 | 利確 | **なし（TP 廃止、2026-07-05）** | `TARGET_PROFIT_RATE=None` で無効化 |
| 4 | タイムストップ | 保有5営業日経過 | システム通知 → 手動成行売却 |

> **バックテストの保守的扱い**
> 同日に複数の出口条件が成立した場合、バックテストでは損失側（ストップ）を優先計上し期待値の過大評価を防ぐ。
> 実装: `src/squant/domain/position_manager.py` の `evaluate_exit` がこの順序で判定する（TP は rate=None でスキップ）。

### 利確（TP）を廃止した根拠 — 2026-07-05

トレンドフォロー転換後も旧・押し目時代の固定利確を引き継いでいたが、拡張グリッド検証で
**「固定利確は勝ちトレードの上限キャップとして機能し、期待値を削る」**ことが判明:
- 従来グリッド（TP ≤6%）: OOS 平均 +0.64%/月 → 拡張グリッド（TP 8〜10%・なし）: **+1.03%/月**（3/3窓 Robust）
- 全窓の IS 上位を TP 高め/なし × 広いトレール（ATR 2.5〜3.0）が独占
- 利益の伸ばし役はトレーリングストップ（3.0×ATR のラチェット）が担い、下方リスクはハードストップ -2.5% が従来どおり抑える

### リスク・リワード構造（TP なし）

| 項目 | 値 |
|---|---|
| 想定最大損失 | -2.5%（逆指値で機械執行、スリッページ小） |
| 利益上限 | なし（トレーリング 3.0×ATR で利を伸ばす） |
| 保有上限 | 5営業日（タイムストップ） |
| 実測 R:R | 平均利益 ≈ 平均損失の 1.4〜1.7 倍（2022-25 固定適用、win率 36〜40%） |

### バックテスト実測（採用値 noTP/ATR3.0/TS5、¥600,000・2銘柄・上限¥3,000、固定適用）

| 年 | Trades | 月平均 | PF | Max DD |
|---|---|---|---|---|
| 2022 | 105 | +0.63% | 1.12 | -13.1% |
| 2023 | 88 | -0.04% | 0.99 | -11.2% |
| 2024 | 89 | **+3.08%** | **1.69** | -10.9% |
| 2025 | 96 | **+1.93%** | **1.44** | -8.0% |

- **4年平均 +1.40%/月（≈ 年¥100,800）、最悪年でも収支ほぼゼロ（2023: -0.04%）、最悪DD -13.1% < CB 15%**
- 参考: 拡張グリッド WF（窓別 IS ベスト選定）は 3/3 窓 Robust、OOS 平均 +1.03%/月
- 旧構成の実測（¥200k・W1best 等）は [backtest_report.md §8.12〜8.14](backtest_report.md) に履歴として残す

### パラメータ確定値（noTP/ATR3.0/TS5: 拡張グリッド検証、2026-07-05 適用）

| パラメータ | 確定値 | Grid 探索レンジ | 採用根拠 |
|---|---|---|---|
| ハードストップ | -2.5% | （固定） | 最大損失制約を維持 |
| 利確目標 | **なし（TP 廃止）** | +4% 〜 +10% / なし | 固定利確は勝ちを削ると実測（§利確を廃止した根拠） |
| ATR乗数 | **3.0** | 1.5 〜 3.0 | TP なし運用では広いトレールが利を伸ばす。W1/W3 の IS 上位に横断出現 |
| トレーリング基準 | 直近高値 - 3.0×ATR | （固定） | 終値ベースはラチェット不成立 |
| ATR期間 | 14 | （固定） | — |
| RSI(14)上限/下限 | 60 / 35 | grid から除外 | **C 戦略ではフィルタ未使用**（ランキングの RSI 昇順のみ使用、A1 の遺構） |
| タイムストップ | 5営業日 | 5 〜 15 | 10/15 日も検証したが 5 日が窓横断で安定 |
| 出来高サージ倍率 | 1.2 | （固定） | — |

> **本番パラメータの変遷**
> ① A1 流用（TP6/ATR1.5/TS5）→ C 戦略で劣後判明（§8.12）
> ② 2026-07-04 W1best（TP5/ATR2.5/TS5）に変更（¥200k 時代）
> ③ 2026-07-05 予算 ¥600k 化に伴い拡張グリッドで再選定 → **noTP/ATR3.0/TS5**（4年固定 +1.40%/月・最悪DD -13.1%、候補比較は選択バイアスを含む点に注意）。ハードストップ -2.5% は一貫して不変。詳細: [backtest_report.md §8.14](backtest_report.md)。

> **用語**
> - *ATR（Average True Range）*: 過去N日間の真の値幅平均。銘柄ごとのボラティリティに応じてストップ幅を自動調整。
> - *OCO（One-Cancels-the-Other）*: 利確指値と損切り逆指値を同時に出し、片方が約定したら他方を自動キャンセルする注文方式。
> - *トレーリングストップ*: 上昇につれてストップを切り上げ、利益を守りながらトレンドを追いかける仕組み。下落時は下がらない（ラチェット）。

---

## リスク管理

### サーキットブレーカー（ポートフォリオ全体）

ポートフォリオ全体の **フェーズ開始来の累積実現損失** が **投資資本の15%（¥600,000 × 15% = ¥90,000）** を超えると全取引を停止し、Slack に通知する。再開は手動リセットのみで、リセット前に Walk-Forward 再検証を必須とする（詳細は [requirements.md §サーキットブレーカーの定義](requirements.md)）。
**個別銘柄ではなく合計で判定**するため、1銘柄で大損が出ても他銘柄が稼いでいれば停止しない。
実装は絶対額（`CIRCUIT_BREAKER_LOSS_JPY = ¥30,000`）で判定する。

### ステートマシン（複数 Position 対応）

```
IDLE ⇄ ACTIVE（空きスロット > 0 かつ 既保有 ≥ 0）
ACTIVE → SETTLING（最後の Position を出口）→ IDLE
```

各 Position は独立の状態を持ち、Google Sheets の `portfolio` タブに **複数行**で永続化される。
**保有銘柄重複ガード**: スクリーナーは現在の `held_tickers` 集合を引数に取り、シグナル候補から除外する。
**二重エントリー防止**: 同一 ticker が pending_signal にすでに存在する場合、追加検出時はスキップする（べき等性）。

### Phase 1 で特に監視する事項

B-WF（3銘柄分散版での Walk-Forward）後のベースラインとの乖離を計測:

- 月次エントリー回数（2銘柄分散ぶん、単一銘柄期より増加見込み）
- 月次リターン（B-WF ベンチマーク次第、目標は月+0.5%以上 = 年¥100k／Phase 1 ベース）
- 勝率（A1 単一銘柄実測 44.9% / 分散による変化を要観測）
- 約定スリッページ（複数銘柄同時発注時の運用負荷含む）
- 寄りギャップダウンの頻度・規模（銘柄ごとに独立）
- トレーリングストップ更新の手動運用負荷（銘柄数 × 営業日 で線形増加）
- **保有銘柄重複ガードの実効性**（実運用で同一ticker候補が出てこないこと）

実測がバックテストを下回り続ける場合は、戦略本体（シグナル条件 or ユニバース）の見直しを検討。

---

## 付録: 機能フェーズへの拡張（2026-07-05 改定 — 資本拡大は終了）

投資資本は **¥600,000 で固定**（オーナー決定・実測根拠は requirements.md 付録参照）。以後の拡張は資本ではなく機能で行う。

| 項目 | 現行 | F1: ユニバース再構築 | F2: 3銘柄分散化 |
|---|---|---|---|
| 投資資本 | **¥600,000（固定）** | 変更なし | 変更なし |
| 同時保有銘柄数 | **2銘柄** | 2銘柄 | 3銘柄（要 WF 検証） |
| 対象株価帯 | ¥100〜¥3,000 | ¥100〜¥3,000（母集団を東証全体から再選定） | ¥100〜¥2,000（枠 ¥200k 化） |
| 1銘柄最大損失（-2.5%） | 約 ¥7,500 | 変更なし | 約 ¥5,000 |
| サーキットブレーカー | **¥90,000（15%）** | 変更なし | 変更なし |
| 主な作業 | — | universe.csv 再生成（J-Quants 上場銘柄一覧 + ファンダ取得）、生存バイアス管理、WF 再検証 | `MAX_POSITIONS=3`・PRICE_MAX 変更、WF 再検証、発注運用負荷の再評価 |

### 機能フェーズ着手時のチェックリスト

1. [requirements.md §運用判定基準](requirements.md) の「良好」判定（累計30取引以上 かつ 月平均 > +0.7%）
2. WF 再実行（`scripts/walk_forward.py`、in-process デフォルト）で新構成でも過半数窓 OOS robust
3. 出口パラメータは新構成の grid で再選定（価格帯が変わると最適出口も変わることが実証済み — §8.13.2）
4. SBI証券側の取引履歴・残高と Google Sheets の複数行 portfolio タブの同期確認
