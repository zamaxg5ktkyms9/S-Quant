# 残件一覧(2026-07-10 時点、07-11 更新)

> 独立レビュー対応（F-1/F-2/F-3）完了時点でのバックログ。
> 完了したら本ファイルから削除し、大きな判断は requirements / backtest_report に記録する。

## 1. オーナー作業（あなたにしかできないもの）

| # | 残件 | 内容 | 優先度 |
|---|---|---|---|
| O-1 | **Slack 報告用チャンネル作成** | 新チャンネル作成 → ブリッジ bot（SLACK_BRIDGE_BOT）を招待 → チャンネル ID を Claude に伝える。`.env` への `SLACK_REPORT_CHANNEL_ID` 追記は Claude が実施。コード側は対応済み（session_to_slack.py、未設定時は従来 webhook にフォールバック中） | 中 |
| O-2 | gh auth login | GHA ログを Claude が直接読めるようにする（ブラウザ対話が必要なため Claude 不可）。A1 watchdog / A2 ファネル記録の導入で**必要性は低下** — 急がない | 低 |
| O-3 | 2201.T の出口対応（継続運用） | TRAILING UPDATE 通知が来たら SBI の逆指値を更新、TIME_STOP 通知（7/16 頃見込み）が来たら翌朝成行売却 → Slack に売却報告 | 運用 |

## 2. Claude 実装残

| # | 残件 | 内容 | 工数見込み |
|---|---|---|---|
| V-4 | Monte Carlo 分布推定 | 4年トレード列のブートストラップ数千回で「CB 1年内発動確率」「年間リターン90%区間」を算出、運用判定基準を確率閾値で再定義。グリッド反復履歴の多重検定割引も含む | 半日 |
| B-3 | 四半期 WF 定例化 | rolling 3窓 WF を四半期自動実行し Robust 維持を確認（パラメータ劣化の早期検知）。結果は Slack へ | 半日（下記設計メモ反映後） |

**検証強化パッケージ（2026-07-11 オーナー承認、推奨順 V-2 → V-1 → Opus移譲(V-3) → V-4）**:
- V-1（日次パリティ照合）: **2026-07-12 完了** — scripts/parity_check.py + Daily Parity Check workflow（火〜土 01:20 JST）。exit parity（バックテスト出口モデルをエントリー日から再生し本番記録と照合）/ entry parity（寄付約定・ギャップ見送り判定）/ scan parity（IDLE スキャン日の独立再取得・再実行で funnel_log 件数を再現）。乖離は severity（ok/info/alert）分類し parity/parity_log.csv に日次記録、alert のみ Slack 通知。運用の読み方は operational_notes §10。**初回実行（対象 7/10）で実乖離を検出**: モデルは 2201.T の金曜ザラ場ストップ約定（安値 ¥2,649.0 ≤ トリガー ¥2,649.56）を予測、本番は終値判定で HOLDING — SBI 実約定の有無をオーナーに確認依頼中
- V-2（会計恒等式チェック）: **2026-07-11 完了（4c7b295）** — scripts/verify_ledger.py を watchdog 後段（23:45 JST）で毎晩実行。「累積PnL = Σtrades」「現金 + 保有原価 = 初期資本 + 実現損益」を Decimal 厳密検証、違反で Slack アラート + workflow 赤
- V-3（mutation testing + property-based）: **2026-07-11 完了（Opus 実装 706c854、Fable が独立検証済み）**。資金4ファイル kill rate 65.3%→86.3%（CB/slippage=100%）、Hypothesis 不変条件3件、手順は operational_notes §9。検証で kill 数 373 を全再実行で再現、等価変異分類をサンプル監査（qc 3/3・pm 8/33）で妥当と確認。残留メモ: TP 境界変異（intraday_high >= vs > tp_price 等）が生存 — 本番は TP 無効で無害だが、グリッドで TP を再検証する際はバックテストの厳密約定境界に影響し得る
- V-5（異種モデルレビュー）: オーナー操作。GPT/Gemini 系に四半期1回、同じレビュープロンプトを投入（Claude 同士の相関盲点の相殺）
- 原則: 過去データへのグリッドサーチ追加は F-3 再発リスクのため回数上限を設ける（要オーナー合意）

**B-3 設計メモ（2026-07-11 調査済み・次スレッドで実装）**:
1. walk_forward.py の窓は `DEFAULT_WINDOWS` 固定（2022〜2025）のため、四半期実行しても毎回同じ結果になる。**実行日基準で窓をスライドさせるオプション（例: `--anchor-today` — IS/OOS を today−4y〜today の1年刻み3窓に自動計算）の追加が前提**
2. in-process モードは `.backtest_cache/data_*.pkl`（ローカル 57MB）必須。GHA 上では backtest.py でのキャッシュ生成（J-Quants 4年分フェッチ）が必要 — **所要時間未実測**。actions/cache + 初回 workflow_dispatch での実測を先に行うこと（walk-forward Skill の --benchmark 原則）
3. 四半期 cron の初回は 2026-10-01 なので急ぎではない。Slack 配信は verdict + 窓別 PF/月次（walkforward_*.json のパース）

※ B-2（pandas 断片化解消）は 2026-07-11 完了（00069e3）: signal_engine.with_volume_columns（pd.concat 化・旧代入と等価性テスト済み）に3箇所を集約
※ A-5（約定催促 + 確定 CLI）は 2026-07-11 完了: scripts/remind_pending.py + Pending Fill Reminder workflow（平日 15:30 JST）+ scripts/confirm_entry.py。運用手順は operator_guide §2.4
※ A-3（スリッページ台帳）は 2026-07-11 完了: slippage_log タブ（adverse-positive 符号）、エントリーは確定時に自動記録、出口は scripts/confirm_exit.py、週次集計 scripts/slippage_report.py + Slippage Weekly Report workflow（金曜 21:15 JST）。運用手順は operator_guide §2.4b
※ A-6（Sheets 日次バックアップ）は 2026-07-11 完了: scripts/backup_sheets.py + Sheets Daily Backup workflow（火〜土 00:30 JST、差分時のみ bot コミット）。全9タブ → backups/sheets/*.csv、日次履歴は Git が保持。復旧は該当コミットの CSV を手動で貼り戻す（bootstrap_sheet.py --apply は使わない）
※ A-4（週次サマリー通知）は 2026-07-11 完了: scripts/weekly_summary.py + Weekly Summary workflow（土曜 10:00 JST — 金曜ランの22〜24時完走を確実に取り込む解釈）。weekly_log タブに評価額・TOPIX(1306.T) 終値を毎週スナップショットし、前週比で対TOPIX超過を算出（初回は基準記録のみ）。入出金週は note 列に手動記録する運用

## 3. オーナー判断待ち（着手条件つきの大物）

| # | 残件 | 内容 | 着手条件 |
|---|---|---|---|
| F1 | **ユニバース再構築（ポイントインタイム）** | 東証全体から ¥100〜3,000 帯で機械生成 + 四半期自動更新。**各時点の上場銘柄で backtest（上場廃止含む）・可能ならファンダも時点参照** — 生存者バイアスの部分緩和。**年¥100k 目標の到達はこれが前提**（正準推定は現行 +0.35%/月 = 年¥25k） | 運用判定基準の「良好」判定 or ユニバース健全性トリガー。工数 1〜3日 |
| P-1 | **現実的モデルでのパラメータ再選定** | 現行 noTP/ATR3.0/TS5 は旧約定モデルで選定された値。gap-aware モデルでは最適解が変わる可能性（タイトなストップほどギャップに弱い）。**operational_notes §8 ルール準拠**: 選定後の未使用期間検証を必須とする | F1 と同時が効率的（ユニバースが変わると再選定になるため）。単独なら 2〜4時間+検証 |
| C-2 | レジームフィルタ | 全検証で一貫する唯一の弱点「2022〜23型レジームで負ける」への対処。指標1つ・閾値1つの最小構成で WF 検証 | F1/P-1 の後。0.5〜1日 |
| F2 | 3銘柄分散化 | ¥200k/枠・上限¥2,000。DD 平滑化目的 | F1 完了後、WF Robust 確認できた場合のみ |

## 4. 既知の技術的負債（低優先・記録のみ）

| # | 内容 | 状態 |
|---|---|---|
| D-1 | `scripts/` の ruff 既存指摘 7件（F541/N812 等） | CI ゲート対象外。一括修正は diff 汚染のため意図的に放置 |
| D-2 | mypy 55 エラー | ゲート外（既知）。新規コードで増やさない運用 |
| D-3 | 出口シミュレーションの `loc[:today]` スライスコピー | precompute 導入で実害は微小。将来の高速化余地 |
| D-4 | `JQuantsClient._RateLimiter` のプロセスローカル性 | キャッシュ未生成期間の並列実行時のみ顕在化（backtest_report Appendix D-1） |
| D-5 | reconcile の時間ベース time-stop 警告が単一 position 前提のログ文言 | 実害なし（daily_runner._reconcile）。複数 position 化の残渣 |

## 4.5 アイデアメモ（オーナー発案・実装ネタ枯渇時のオプション）

- 開発記事のまとめ作成を Claude に任せる（NotebookLM との棲み分け: リポジトリの実コード・実データを引用した正確な記事化は Claude 側の利点）

## 5. 直近の確認事項（今夜〜今週）

- [x] funnel_log 初回記録（7/10 確認）
- [ ] 月曜 7/13 run: **2201.T の HOLD/TRAILING 通知が正常に届く**こと（holding 取得窓バグ修正 8e12ef5 の実地確認）
- [ ] 今夜 23:45 の **watchdog 初回実行**が緑で完走すること（GitHub Actions）
- [ ] 修正版 CB での初回 run が正常（発動なし・cumulative_loss は 2201.T の出口確定まで 0 のまま）
- [ ] 2201.T 決済時: trades タブへの記録と CB カウンタの純損益反映を確認（修正後初の実データ）
