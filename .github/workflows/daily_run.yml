name: Daily Trading Run

on:
  # 20:30 JST = 11:30 UTC. The previous "15 11" slot suffered repeated
  # multi-hour GitHub schedule lag (6/1 cron skipped entirely, 6/2 cron ran
  # 4h late at 00:11 JST on 6/3) because */15 minute slots are highly
  # contested. "30 11" is less congested and still leaves enough headroom
  # before SBI's next-day open at 09:00 JST.
  schedule:
    - cron: "30 11 * * 1-5"
  # Manual trigger for testing
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no writes to Sheets)"
        required: false
        default: "true"
        type: boolean
      bypass_execution_time_guard:
        description: "テスト: 20:00 JST ガード無視（本番 cron では false 固定）"
        required: false
        default: "false"
        type: boolean
      bypass_trading_day_check:
        description: "テスト: TSE 営業日チェック無視（本番 cron では false 固定）"
        required: false
        default: "false"
        type: boolean

jobs:
  trade:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GCP_SA_KEY_JSON: ${{ secrets.GCP_SA_KEY_JSON }}
      SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      JQUANTS_API_KEY: ${{ secrets.JQUANTS_API_KEY }}
      DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
      BYPASS_EXECUTION_TIME_GUARD: ${{ github.event.inputs.bypass_execution_time_guard || 'false' }}
      BYPASS_TRADING_DAY_CHECK: ${{ github.event.inputs.bypass_trading_day_check || 'false' }}
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -e .

      - name: Run daily pipeline
        run: python -m squant.main

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08  # v4.6.0
        with:
          name: failure-logs-${{ github.run_id }}
          path: "*.log"
          retention-days: 7
